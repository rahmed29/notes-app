export { setListInMemory, setImageList, listInMemory, imageList };

let listInMemory;
let imageList;

function setListInMemory(list) {
  listInMemory = list;
}

function setImageList(list) {
  imageList = list;
}
